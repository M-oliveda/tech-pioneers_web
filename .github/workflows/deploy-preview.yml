name: Deploy to Preview

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [develop, main]

env:
  IMAGE_NAME: techpioneers

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: preview
      url: ${{ steps.preview-url.outputs.url }}
    permissions:
      pull-requests: write
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate preview service name
        id: preview-name
        run: |
          PR_NUMBER=${{ github.event.pull_request.number }}
          SERVICE_NAME="techpioneers-pr-${PR_NUMBER}"
          echo "service_name=${SERVICE_NAME}" >> $GITHUB_OUTPUT
          echo "Preview service: ${SERVICE_NAME}"

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build --target protected \
            --build-arg AUTH_USERNAME=${{ secrets.PREVIEW_AUTH_USERNAME }} \
            --build-arg AUTH_PASSWORD=${{ secrets.PREVIEW_AUTH_PASSWORD }} \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:pr-${{ github.event.pull_request.number }} \
            .

      - name: Push Docker image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:pr-${{ github.event.pull_request.number }}

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run (Preview)
        run: |
          gcloud run deploy ${{ steps.preview-name.outputs.service_name }} \
            --image=docker.io/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:pr-${{ github.event.pull_request.number }} \
            --platform=managed \
            --region=${{ secrets.GCP_REGION }} \
            --project=${{ secrets.GCP_PROJECT_ID }} \
            --allow-unauthenticated \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=1 \
            --port=80 \
            --timeout=60 \
            --concurrency=80 \
            --cpu-throttling \
            --tag=pr-${{ github.event.pull_request.number }}

      - name: Get Preview URL
        id: preview-url
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ steps.preview-name.outputs.service_name }} \
            --region=${{ secrets.GCP_REGION }} \
            --project=${{ secrets.GCP_PROJECT_ID }} \
            --format='value(status.url)')
          echo "url=${SERVICE_URL}" >> $GITHUB_OUTPUT
          echo "Preview URL: ${SERVICE_URL}"

      - name: Comment PR with preview URL
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.pull_request.number;
            const previewUrl = '${{ steps.preview-url.outputs.url }}';
            const serviceName = '${{ steps.preview-name.outputs.service_name }}';

            const comment = `## 🚀 Preview Deployment Ready!

            ### 🔗 [**View Preview Deployment →**](${previewUrl})

            <table>
              <tr>
                <td><strong>Environment</strong></td>
                <td>Preview (Ephemeral)</td>
              </tr>
              <tr>
                <td><strong>Service</strong></td>
                <td><code>${serviceName}</code></td>
              </tr>
              <tr>
                <td><strong>URL</strong></td>
                <td><a href="${previewUrl}">${previewUrl}</a></td>
              </tr>
              <tr>
                <td><strong>Commit</strong></td>
                <td><code>${context.sha.substring(0, 7)}</code></td>
              </tr>
            </table>

            ---

            **🔒 Remember to use have the username and password for authentication**

            **📋 Environment Details:**
            - **Purpose:** Test individual PRs in isolated environments
            - **Data Type:** Synthetic / Fake data
            - **Security Level:** Medium
            - **Who Uses It:** Devs, QA, Product

            > 💡 **Note:** This preview environment will be automatically cleaned up when the PR is closed or merged.
            `;

            github.rest.issues.createComment({
              issue_number: prNumber,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
